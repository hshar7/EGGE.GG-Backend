type User {
    id: String!
    publicAddress: String!
    name: String
    email: String
    organization: AbstractOrganization
    avatar: String!
    createdAt: Date!
    updatedAt: Date!
}

extend type Query {
    user(id: String): User
    myProfile: User
    usersByOrganization(organizationId: String): [User]
}
